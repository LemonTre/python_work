numbers = [number for number in range(1,1000001)]

#get the min
min_data = min(numbers)
print(min_data)

#get the max
max_data = max(numbers)
print(max_data)

#get the sum
sum_data = sum(numbers)
print(sum_data)